visibilitychange handler in page.tsx (lines 166-187):

CONFIRMED: refetch() and refetchUser() are retained.
CONFIRMED: loadKeywordCount() and loadKeywordNoticesSilent() are removed.
CONFIRMED: Context (NotificationBadgeContext) handles keyword refresh via its own visibilitychange listener.

Dependency array: [filter, isLoggedIn, refetch, refetchUser]
- loadKeywordCount: REMOVED
- loadKeywordNoticesSilent: REMOVED

Handler code:
  if (document.visibilityState === 'visible') {
    if (isLoggedIn) { refetchUser(); }
    if (filter === 'ALL') { refetch(); }
  }
