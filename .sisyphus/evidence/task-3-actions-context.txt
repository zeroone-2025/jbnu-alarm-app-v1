useNoticeActions.ts Context consumption:

CONFIRMED: setKeywordNotices parameter removed from function signature.
CONFIRMED: useNotificationBadge() called inside hook to get setKeywordNotices.

New signature: useNoticeActions(isLoggedIn: boolean)
Old signature: useNoticeActions(isLoggedIn: boolean, setKeywordNotices: Dispatch<SetStateAction<Notice[]>>)

Import added: import { useNotificationBadge } from '@/_context/NotificationBadgeContext';
Import removed: import { Dispatch, SetStateAction } from 'react';

Inside hook body:
  const { setKeywordNotices } = useNotificationBadge();

page.tsx call site updated from:
  useNoticeActions(isLoggedIn, setKeywordNotices)
to:
  useNoticeActions(isLoggedIn)
